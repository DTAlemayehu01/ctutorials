//Stack ADT
//Types are not restricted to the specified types, they can be anything

class Stack{
	private:
		type top
		int maxSize; //max size possible for stack
		int count; //current # of elements
	private:
		void initatlize() //Initalize stack adt
		bool = bool isFull();
		bool = isEmpty();
		value = peek(); //return top value of stack
		void push(item); //put on stack
		void pop(); // remove item from stack
		void disp();
}
