struct Node {
	string key;
	Node *next;
};

class SLL {
	private:
		//head-ptr to first element in list. null means empty list.
	public:
		//initalize() - set header to null
		//nodePtr = search(value) - find a value and return ptr to its node
		//insertNode(previousPtr,newName) - given a ptr to a node enter a new ????
		//??????
		SLL(); //constructor declaration
		~SLL(); //destructor declaration
};
